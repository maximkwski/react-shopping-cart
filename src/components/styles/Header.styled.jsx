import styled from "styled-components";

export const StyledHeader = styled.header`
    padding: 1.2em 0;
    border: 1px solid black;

    a {
        display: flex;
        align-items: center;
    }
`